---
- name: include roles for juno release swift-glance
  include: juno.yml
  when: openstack_release == 'juno'

- name: include roles for kilo release swift-glance
  include: kilo.yml
  when: openstack_release == 'kilo'
